This repository contains codes related to nose.

Usage:
    cd to the dir of the test cases.

    run
        nosetests <testcase.py>
    if need detail info, add -v,
    if need print content of class or function be tested add -s

